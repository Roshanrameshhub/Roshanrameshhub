name: LeetCode Telemetry Sync

on:
  schedule:
    # Run every Sunday at 00:00 UTC
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-leetcode:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Fetch Live LeetCode Stats
        run: |
          python scripts/fetch_leetcode.py

      - name: Regenerate Assets with Live Telemetry
        run: |
          python scripts/generate_svgs.py

      - name: Commit & Push Updates
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/leetcode.json assets/leetcode/
          if git diff --staged --quiet; then
            echo "No telemetry changes to commit."
          else
            git commit -m "chore(telemetry): sync LeetCode stats & refresh charts [skip ci]"
            git push
          fi
